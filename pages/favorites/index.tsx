import { Layout } from '../../components/layouts';

const FavoritePages = () => {
    return (
        <Layout title='Pokemones - Favorites'>
            <h1>Favorites</h1>
        </Layout>
    );
};

export default FavoritePages;
